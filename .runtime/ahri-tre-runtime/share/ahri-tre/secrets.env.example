# Placeholder-only optional secrets for the profile above.
# Ordinary identity-bound opens should not need direct PostgreSQL passwords
# or DuckLake catalog credentials. Uncomment only when your operator instructs
# you to provide a temporary local secret.
# ORCID_CLIENT_SECRET=<oauth-client-secret-if-used>
